# Rust
/target

# Tauri generates these from tauri.conf.json on every build.
/app/gen

# Front end
/ui/node_modules
/ui/dist

# Local scratch
*.log

/docs
# Local test deployment (app + TAP DLL + debug log/flag) and crash dumps.
/dist
/dumps
