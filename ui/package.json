{
  "name": "winbeautify-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "WinBeautify front end: widget bar, flyout and settings centre.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:only": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.12"
  }
}
